#include <stdio.h>

//needs to return an int, and it takes two parameters: an into for the argument count and an array of char * string for the arguments
int main(int argc, char *argv[])
{ //block
  int distance = 100; //declaration + arrignment

  printf("You are %d miles away.\n", distance);

  return 0; //unix exit value
}
